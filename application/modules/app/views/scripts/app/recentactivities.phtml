<?php if(empty($this->activities)){
	echo "No Recent Activities";
}

	$count = 0;
	foreach ($this->activities as $act):	
		if ($count % 2 == 0){ ?>			
		<li class="grey-light" >
		<? }else{ ?>
		<li class="grey-dark" >
		<? }

		 switch($act->activity_type){ 
		
			case 'follow': ?>
				You followed <a href="#" onclick="userProfile('<?=$act->event_object?>')"><?=$act->target_user_name?></a> on <?=date("l, F d, g:i a",strtotime($act->created_time))?>.
				<?break;
			case 'unfollow' :?>
				You unfollowed <a href="#" onclick="userProfile('<?=$act->event_object?>')"><?=$act->target_user_name?></a> on <?=date("l, F d, g:i a",strtotime($act->created_time))?>.
				<?break;	
			case 'like-status':?>
				You liked <?=$act->target_user_name?>'s <a href="#" onclick="comment_bubble('<?=$act->event_object?>', 5)">status</a> post on <?=date("l, F d, g:i a",strtotime($act->created_time) )?>.
				<?break;
			case 'unlike-status':?>
				You unliked <?=$act->target_user_name?>'s <a href="#" onclick="comment_bubble('<?=$act->event_object?>' , 5)">status</a> post on <?=date("l, F d, g:i a",strtotime($act->created_time))?>.
				<?break;
			case 'like-comment':?>
				You liked <?=$act->target_user_name?>'s <a href="#" onclick="comment_bubble('<?=$act->event_object?>' , 5)">comment</a> on <?=date("l, F d, g:i a",strtotime($act->created_time))?>.
				<?break;
			case 'unlike-comment':?>
				You unliked <?=$act->target_user_name?>'s <a href="#" onclick="comment_bubble('<?=$act->event_object?>' , 5)">comment</a> on <?=date("l, F d, g:i a",strtotime($act->created_time))?>.
				<?break;
			case 'post-comment':?>
				You post a comment on <?=$act->target_user_name?>'s <a href="#" onclick="comment_bubble('<?=$act->event_object?>', 5)">post</a> on <?=date("l, F d, g:i a",strtotime($act->created_time))?>.
				<?break;
			case 'post':?>
				You made a post on <?=$act->target_user_name?>'s Fanpage on <?=date("l, F d, g:i a",strtotime($act->created_time))?>.
				<?break;
			default:
			
		}?>
		
		</li>
	<?endforeach; ?>

